---
interface Props {
  title: string;
}

import { getEntry } from "astro:content";

const layout = await getEntry("layout", "data");
const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={layout.data.head.description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href={layout.data.head.favicon} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="flex flex-col min-h-screen"> <!-- insert class="background" here -->
    <slot />
  </body>
</html>

<style>
  /* .background {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    background-color: rgba(42, 42, 170, 0.9);
    z-index: -1;
  }

  .background::before,
  .background::after {
    content: "";
    position: absolute;
    width: 100%;
    height: 100%;
    background: radial-gradient(
        circle at 10% 30%,
        rgba(197, 55, 55, 0.1),
        transparent 50%
      ),
      radial-gradient(circle at 80% 60%, rgba(82, 199, 82, 0.3), transparent 50%),
      radial-gradient(circle at 50% 80%, rgba(82, 82, 213, 0.6), transparent 50%);
    filter: blur(100px);
    z-index: -1;
  }

  .background::after {
    filter: blur(200px);
    opacity: 0.7;
    z-index: -1;
  } */

  .background {
    background: url("../../public/global/background.jpg") no-repeat
      center center fixed;
    background-size: cover;
  }
</style>
